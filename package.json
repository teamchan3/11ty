{
  "name": "11ty-project",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:dev": "tsx node_modules/@11ty/eleventy/cmd.cjs --config=eleventy.config.ts --serve --incremental",
    "build:pro": "tsx node_modules/@11ty/eleventy/cmd.cjs --config=eleventy.config.ts",
    "clean:dev": "rimraf dist",
    "clean:build": "rimraf build",
    "clean:all": "rimraf dist build",
    "dev": "npm run clean:dev && npm run build:dev",
    "build": "npm run clean:build && npm run build:pro"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "type": "module",
  "dependencies": {
    "@11ty/eleventy": "^3.0.0-beta.1",
    "jsx-async-runtime": "^0.5.2"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "autoprefixer": "^10.4.20",
    "clsx": "^2.1.1",
    "concurrently": "^9.0.1",
    "esbuild": "^0.24.0",
    "glob": "^11.0.0",
    "postcss": "^8.4.47",
    "postcss-cli": "^11.0.0",
    "postcss-import": "^16.1.0",
    "postcss-nesting": "^13.0.0",
    "postcss-preset-env": "^10.0.6",
    "prettier": "^3.3.3",
    "rimraf": "^6.0.1",
    "tailwindcss": "^3.4.13",
    "tsx": "^4.19.1"
  }
}
